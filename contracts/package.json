{
  "name": "@pramana/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "PRAMANA permissioned-chain contracts. Standalone package \u2014 not part of the root workspaces, so the demo installs fast.",
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy": "hardhat run scripts/deploy.ts --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.17",
    "typescript": "~5.7.3",
    "@types/node": "^22.10.5"
  }
}
